import 'package:flutter/material.dart';

class PageMenu extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Menu", textScaleFactor: 2.0,),
      
    );
  }
}