import 'package:flutter/material.dart';

class PageCardapio extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Cardapio", textScaleFactor: 2.0,),
      
    );
  }
}