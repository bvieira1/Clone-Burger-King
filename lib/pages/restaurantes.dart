import 'package:flutter/material.dart';

class PageRestaurante extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Restaurantes", textScaleFactor: 2.0,),
      
    );
  }
}