import 'package:flutter/material.dart';

class PageCupons extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Cupons", textScaleFactor: 2.0,),
      
    );
  }
}